const express = require("express");
const app = express();

const products = [{

  id:1;
  title
}, {}];

app.get("/", (req, res) => {
  res.send({ id: 5 });
});

app.listen(1307, () => console.log(""));
